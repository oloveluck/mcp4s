services:
  # MCP Calculator Server
  calculator-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health"]
      interval: 5s
      timeout: 3s
      retries: 5

